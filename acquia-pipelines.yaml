version: 1.3.0
services:
  - composer:
      version: 1
  - mysql
  - php:
      version: 8.3

variables:
  global:
    COMPOSER_BIN: $SOURCE_DIR/vendor/bin
events:
  build:
    steps:
        - setup-env:
            type: script
            script:
              - composer --version
